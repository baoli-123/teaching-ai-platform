app:
  redis:
    enabled: true

spring:
  data:
    redis:
      host: localhost
      port: 6379
      password: ""
      lettuce:
        pool:
          max-active: 32
          max-idle: 16
          min-idle: 4
